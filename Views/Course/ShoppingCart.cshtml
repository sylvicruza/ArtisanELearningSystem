@model ArtisanELearningSystem.Entities.Course

@{
    ViewData["Title"] = "ShoppingCart";
}

<div class="_215b15">
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <div class="title125">
                    <div class="titleleft">
                        <div class="ttl121">
                            <nav aria-label="breadcrumb">
                                <ol class="breadcrumb">
                                    <li class="breadcrumb-item"><a href="#">Home</a></li>
                                    <li class="breadcrumb-item active" aria-current="page">Shopping Cart</li>
                                </ol>
                            </nav>
                        </div>
                    </div>
                </div>
                <div class="title126">
                    <h2>Shopping Cart</h2>
                </div>
            </div>
        </div>
    </div>
</div>
<div class="mb4d25">
    <div class="container">
        <div class="row">
            <div class="col-lg-8">
                <div class="fcrse_1">
                    <a asp-controller="Course" asp-action="CourseDetailview" asp-route-id="@Model.Id" class="hf_img">
                        <img class="cart_img" src="/curus/images/courses/img-2.jpg">
                    </a>
                    <div class="hs_content">
                        <div class="eps_dots eps_dots10 more_dropdown">
                            <a href="#"><i class="uil uil-times"></i></a>
                        </div>
                        <a asp-controller="Course" asp-action="CourseDetailview" asp-route-id="@Model.Id" class="crse14s title900 pt-2">@Model.Title</a>
                        <a href="#" class="crse-cate">@Model.Category | @Model.Level</a>
                        <div class="auth1lnkprce">
                            <p class="cr1fot">By <a href="#">@Model?.Instructor?.Name</a></p>
                            <div class="prce142">
                                @{
                                    decimal price1 = Model?.Price ?? 0;
                                    decimal discount1 = Model?.Discount ?? 0;
                                    decimal total1 = price1 - discount1;
                                }
                                $@total1.ToString("0.00")
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <div class="col-lg-4">
                <div class="membership_chk_bg rght1528">
                    <div class="checkout_title">
                        <h4>Total</h4>
                        <img src="/curus/images/line.svg">
                    </div>
                    <div class="order_dt_section">
                        <div class="order_title">
                            <h4>Orignal Price</h4>
                            <div class="order_price">$@Model?.Price</div>
                        </div>
                        <div class="order_title">
                            <h6>Discount Price</h6>
                            <div class="order_price">$@Model?.Discount</div>
                        </div>
                        <div class="order_title">
                            <h2>Total</h2>
                            <div class="order_price5">
                                @{
                                    decimal price = Model?.Price ?? 0;
                                    decimal discount = Model?.Discount ?? 0;
                                    decimal total = price - discount;
                                }
                                $@total.ToString("0.00")
                            </div>
                        </div>

                       @* <div class="coupon_code">
                            <p>Learn now is applied.</p>
                            <div class="coupon_input">
                                <div class="ui search focus mt-15">
                                    <div class="ui left icon input swdh11 swdh19">
                                        <input class="prompt srch_explore" type="text" name="couponcode"  id="id_coupon_code" required maxlength="6" placeholder="Enter Coupon Code">
                                    </div>
                                    <button class="code-apply-btn" type="submit">Apply</button>
                                </div>
                            </div>
                        </div>*@
                        <form method="post" asp-controller="Enrollment" asp-action="Enroll" asp-route-courseId="@Model?.Id">
                            <button class="chck-btn22" type="submit">Checkout Now</button>
                        </form>
                        @*<a href="#" class="chck-btn22">Checkout Now</a>*@
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
    }
